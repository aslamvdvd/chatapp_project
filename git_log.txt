commit 2599710a7bb7d617657c3118723a6fe1a9a11e64
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Wed Jun 4 16:22:55 2025 +0530

    feat: Implement Email Signup Flow with Docker Fixes & UI/UX Enhancements
    
    This commit encompasses a significant set of changes to establish the backend Docker environment, refactor the Android email signup flow, and implement various UI/UX improvements and bug fixes for the signup screen.
    
    **1. Backend Docker & Rust Environment Configuration:**
    
    *   **Rust Version Compatibility:** Resolved `Cargo.lock` version conflicts by updating the backend `Dockerfile` to use `rust:slim` (from `rust:1.77-slim` then `rust:1.78-slim`), ensuring compatibility with project dependencies.
    *   **Dockerfile Binary Execution:** Corrected `COPY` and `CMD` instructions in the `backend/Dockerfile` to use the actual compiled binary name (`chatapp_by_aarchangel_backend`) as specified in `Cargo.toml`, ensuring the application starts correctly.
    *   **Docker Compose Port Mapping:** Resolved database port conflicts in `docker-compose.yml` by mapping the host port `5433` to the `db` service's container port `5432`.
    *   **Docker Compose Runtime Command:** Commented out the `command: ["cargo", "watch", ...]` for the backend service in `docker-compose.yml`. This allows the `Dockerfile`'s `CMD` (which executes the pre-compiled binary) to take precedence, as the slim Debian runtime image does not include `cargo`.
    
    **2. Android Signup Flow Refactoring & Consolidation:**
    
    *   **Unified Signup Screen:** Shifted from a multi-step email/password/details entry to a single, consolidated `EmailSignUpScreen` for a more streamlined user experience.
        *   Renamed `CreateAccountDetailsScreen.kt` to `EmailSignUpScreen.kt` and its associated composable.
        *   Updated `AppScreen.kt` navigation graph: `CreateAccountDetails` renamed to `EmailSignUpScreen` with a simplified route (`email_signup/{flowType}`). Removed `EmailEntry` and `PasswordEntry` screens.
        *   `AuthOptionsViewModel` now navigates directly to `EmailSignUpScreen`.
        *   Removed obsolete `EmailEntryScreen.kt` and `PasswordEntryScreen.kt` related code from `MainActivity.kt`.
    *   **ViewModel Logic (`EmailAuthViewModel.kt`):**
        *   `onCreateAccountDetailsContinue()` refactored into `onSignUpAttempt()` to handle validation and submission logic for all fields on the consolidated form.
        *   Removed deprecated `onEmailContinue()`.
    
    **3. Android `EmailSignUpScreen.kt` Enhancements & Fixes:**
    
    *   **Date of Birth (DOB) Field:**
        *   **Dual Input Mode:** Users can now select DOB via the `DatePickerDialog` or type it manually.
        *   **Visual Transformation:** Implemented `DateVisualTransformation.kt` to auto-format manually typed DOB as "DD-MM-YYYY" with hyphens, improving input experience.
            *   Iteratively refined `OffsetMapping` logic within `DateVisualTransformation` to resolve crashes, digit scrambling, and erratic backspace behavior, ensuring correct cursor positioning and input integrity.
        *   **ViewModel Handling:** Added `onDateOfBirthManuallyChanged()` in `EmailAuthViewModel` to process the transformed text field input, simplifying previous re-filtering logic.
        *   **Age Validation:** Implemented COPPA-related age check (>= 13 years old) in `EmailAuthViewModel.onSignUpAttempt()`.
    *   **Gender Selection Field:** Implemented an `ExposedDropdownMenuBox` for gender selection with predefined options.
    *   **Password Requirements Display:** Added dynamic text below the password field to show requirements when focused and no error is active.
    *   **Form Clearing Logic:**
        *   Introduced `clearSignUpForm()` in `EmailAuthViewModel` to reset all input fields and error states.
        *   Form now clears automatically upon navigating back (using either the top app bar icon or the system back button, handled via `BackHandler`), and after a successful (mocked) sign-up.
    *   **General UI:** Updated screen title and descriptive text.
    
    **4. Android Build & Dependency Management:**
    
    *   **Material 3 DatePicker:** Resolved "Unresolved reference" errors for `DatePicker`, `DatePickerDialog`, and `rememberDatePickerState` by:
        *   Upgrading the Compose BOM to `2024.04.01` (from `2023.03.00`).
        *   Updating `androidx.compose.material3:material3` explicitly to `1.2.1`.
        *   Updating `androidx.core:core-ktx` to `1.13.1`, `androidx.lifecycle:lifecycle-runtime-ktx` to `2.8.0`.
        *   Adding necessary import statements to `EmailSignUpScreen.kt`.
    *   **Kotlin Version Compatibility:** Fixed build failures by:
        *   Aligning the Compose Compiler Extension version to `1.5.10`.
        *   Updating the project-level Kotlin plugin in `build.gradle.kts` (root of `android-app`) to `1.9.22` (from `1.9.0`).
    *   **Icon Dependencies:** Ensured `androidx.compose.material:material-icons-extended:1.6.7` was present for password visibility toggle icons.
    *   **Initial Build Errors:** Addressed early experimental API usage in `AppTextField.kt` by adding `@OptIn(ExperimentalMaterial3Api::class)` and correcting `TextFieldDefaults` usage.
    
    This comprehensive set of changes provides a functional Dockerized backend environment and a significantly improved and robust email sign-up experience on the Android client.

commit 29eaf29a3f1bbbd9a84377ad88f56b56ead46a20
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Wed Jun 4 16:21:29 2025 +0530

    feat: Implement Email Signup Flow with Docker Fixes & UI/UX Enhancements
    
    This commit encompasses a significant set of changes to establish the backend Docker environment, refactor the Android email signup flow, and implement various UI/UX improvements and bug fixes for the signup screen.
    
    **1. Backend Docker & Rust Environment Configuration:**
    
    *   **Rust Version Compatibility:** Resolved `Cargo.lock` version conflicts by updating the backend `Dockerfile` to use `rust:slim` (from `rust:1.77-slim` then `rust:1.78-slim`), ensuring compatibility with project dependencies.
    *   **Dockerfile Binary Execution:** Corrected `COPY` and `CMD` instructions in the `backend/Dockerfile` to use the actual compiled binary name (`chatapp_by_aarchangel_backend`) as specified in `Cargo.toml`, ensuring the application starts correctly.
    *   **Docker Compose Port Mapping:** Resolved database port conflicts in `docker-compose.yml` by mapping the host port `5433` to the `db` service's container port `5432`.
    *   **Docker Compose Runtime Command:** Commented out the `command: ["cargo", "watch", ...]` for the backend service in `docker-compose.yml`. This allows the `Dockerfile`'s `CMD` (which executes the pre-compiled binary) to take precedence, as the slim Debian runtime image does not include `cargo`.
    
    **2. Android Signup Flow Refactoring & Consolidation:**
    
    *   **Unified Signup Screen:** Shifted from a multi-step email/password/details entry to a single, consolidated `EmailSignUpScreen` for a more streamlined user experience.
        *   Renamed `CreateAccountDetailsScreen.kt` to `EmailSignUpScreen.kt` and its associated composable.
        *   Updated `AppScreen.kt` navigation graph: `CreateAccountDetails` renamed to `EmailSignUpScreen` with a simplified route (`email_signup/{flowType}`). Removed `EmailEntry` and `PasswordEntry` screens.
        *   `AuthOptionsViewModel` now navigates directly to `EmailSignUpScreen`.
        *   Removed obsolete `EmailEntryScreen.kt` and `PasswordEntryScreen.kt` related code from `MainActivity.kt`.
    *   **ViewModel Logic (`EmailAuthViewModel.kt`):**
        *   `onCreateAccountDetailsContinue()` refactored into `onSignUpAttempt()` to handle validation and submission logic for all fields on the consolidated form.
        *   Removed deprecated `onEmailContinue()`.
    
    **3. Android `EmailSignUpScreen.kt` Enhancements & Fixes:**
    
    *   **Date of Birth (DOB) Field:**
        *   **Dual Input Mode:** Users can now select DOB via the `DatePickerDialog` or type it manually.
        *   **Visual Transformation:** Implemented `DateVisualTransformation.kt` to auto-format manually typed DOB as "DD-MM-YYYY" with hyphens, improving input experience.
            *   Iteratively refined `OffsetMapping` logic within `DateVisualTransformation` to resolve crashes, digit scrambling, and erratic backspace behavior, ensuring correct cursor positioning and input integrity.
        *   **ViewModel Handling:** Added `onDateOfBirthManuallyChanged()` in `EmailAuthViewModel` to process the transformed text field input, simplifying previous re-filtering logic.
        *   **Age Validation:** Implemented COPPA-related age check (>= 13 years old) in `EmailAuthViewModel.onSignUpAttempt()`.
    *   **Gender Selection Field:** Implemented an `ExposedDropdownMenuBox` for gender selection with predefined options.
    *   **Password Requirements Display:** Added dynamic text below the password field to show requirements when focused and no error is active.
    *   **Form Clearing Logic:**
        *   Introduced `clearSignUpForm()` in `EmailAuthViewModel` to reset all input fields and error states.
        *   Form now clears automatically upon navigating back (using either the top app bar icon or the system back button, handled via `BackHandler`), and after a successful (mocked) sign-up.
    *   **General UI:** Updated screen title and descriptive text.
    
    **4. Android Build & Dependency Management:**
    
    *   **Material 3 DatePicker:** Resolved "Unresolved reference" errors for `DatePicker`, `DatePickerDialog`, and `rememberDatePickerState` by:
        *   Upgrading the Compose BOM to `2024.04.01` (from `2023.03.00`).
        *   Updating `androidx.compose.material3:material3` explicitly to `1.2.1`.
        *   Updating `androidx.core:core-ktx` to `1.13.1`, `androidx.lifecycle:lifecycle-runtime-ktx` to `2.8.0`.
        *   Adding necessary import statements to `EmailSignUpScreen.kt`.
    *   **Kotlin Version Compatibility:** Fixed build failures by:
        *   Aligning the Compose Compiler Extension version to `1.5.10`.
        *   Updating the project-level Kotlin plugin in `build.gradle.kts` (root of `android-app`) to `1.9.22` (from `1.9.0`).
    *   **Icon Dependencies:** Ensured `androidx.compose.material:material-icons-extended:1.6.7` was present for password visibility toggle icons.
    *   **Initial Build Errors:** Addressed early experimental API usage in `AppTextField.kt` by adding `@OptIn(ExperimentalMaterial3Api::class)` and correcting `TextFieldDefaults` usage.
    
    This comprehensive set of changes provides a functional Dockerized backend environment and a significantly improved and robust email sign-up experience on the Android client.

commit 3b3238aabd7efe29d66c4d81dd374e10d8c771ff
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Wed Jun 4 11:18:46 2025 +0530

    feat: Implement DB health check and add admin endpoint stubs
    
    - Adds a dedicated /health/db endpoint to verify database connectivity.
    - Refactors health check handlers into  for better organization.
    - Introduces initial stub for admin functionality with an /admin endpoint and corresponding handler/route files (, ).
    - Resolves Utoipa compilation issue for health check endpoints by ensuring proper  decoration and path resolution in OpenAPI documentation.

commit 606508233467f2448108676851c06c29f412857f
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Wed Jun 4 10:41:32 2025 +0530

    Fixing Utopia issue for health_check.

commit d96a469d095fb11b5de9d41205e0719950a51062
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Thu May 29 08:49:46 2025 +0530

    feat(auth): Implement initial user signup endpoint and core backend infrastructure
    
    This commit establishes the foundational backend for the chatapp_by_aarchangel application, focusing on user authentication and core service setup.
    
    Key implementations include:
    
    1.  **User Signup Endpoint (`POST /auth/signup`):**
        *   Defines a new Actix Web route for user registration.
        *   Accepts user details (email, username, name, DOB, gender, password) via a JSON request body.
        *   Implements robust input validation using the `validator` crate for all required fields, including email format, password length, and password confirmation matching.
        *   Provides detailed, field-specific error messages for validation failures.
    
    2.  **Secure Password Handling:**
        *   Integrates the `argon2` crate for strong password hashing.
        *   Passwords are salted and hashed before storage.
        *   Includes utility functions for hashing and verifying passwords.
    
    3.  **Database Integration (PostgreSQL with SQLx):**
        *   Sets up a PostgreSQL database connection pool using `sqlx`.
        *   Defines a `User` model representing the database schema.
        *   Implements logic in `AuthService` to store new users in the `users` table, checking for existing email or username conflicts within a transaction.
        *   Introduces `sqlx-cli` for database migrations, including an initial migration for the `users` table schema. Migrations are now run automatically on application startup.
        *   Configures `DATABASE_URL` via a `.env` file.
    
    4.  **Modular Architecture:**
        *   Organizes the codebase into distinct modules for clarity and maintainability:
            *   `routes`: Defines API routes (e.g., `auth.rs`).
            *   `handlers`: Contains request handling logic (e.g., `auth_handler.rs`).
            *   `services`: Implements business logic (e.g., `auth_service.rs`).
            *   `models`: Defines data structures and DTOs (e.g., `user.rs`).
            *   `db`: Manages database connection setup.
            *   `utils`: Provides shared utilities (e.g., `hash.rs`).
            *   `logging`: Configures structured logging.
        *   Employs dependency injection for services like `AuthService` into handlers.
    
    5.  **Structured Logging with Tracing:**
        *   Implements comprehensive logging using the `tracing` ecosystem (`tracing`, `tracing-subscriber`, `tracing-appender`).
        *   Logs are structured in JSON Lines format (`.jsonl`).
        *   Log files are created daily in a `logs/YYYY-MM-DD/` directory.
        *   Separates logs by type (system, user, admin events) into different files based on trace targets.
        *   Integrates `tracing-actix-web` for request logging.
        *   Existing `println!` and `eprintln!` statements have been converted to use `tracing` macros.
    
    6.  **API Documentation (OpenAPI with Utoipa):**
        *   Sets up `utoipa` to generate an OpenAPI v3 specification.
        *   Integrates `utoipa-swagger-ui` to serve interactive API documentation at `/swagger-ui/`.
        *   Includes schema definitions and examples for DTOs (`SignupUserDto`, `UserPublicData`, `ApiError`).
    
    7.  **Project Setup & Configuration:**
        *   Configures `Cargo.toml` with all necessary dependencies.
        *   Adds a `.gitignore` file tailored for Rust backend projects, including the `logs/` directory.
        *   Application name and descriptions updated to "chatapp_by_aarchangel".
    
    This foundational work enables secure user registration and provides a scalable and maintainable structure for future backend development, including robust error handling, database management, and observability through structured logging and API documentation.

commit 57a54bd7bd5a784cf723497f556e6d4f32fc9d4d
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Sun May 25 10:32:09 2025 +0530

    feat: Restructure project into monorepo and initialize Rust backend
    
    This commit introduces a major restructuring of the project into a monorepo
    layout and sets up the initial Rust backend using Actix Web.
    
    **Project Structure Changes:**
    
    *   **Android Frontend Relocation:**
        *   The entire Android application codebase has been moved from the project root into a dedicated `android-app/` directory.
        *   All associated Gradle files (root `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`, `gradlew`, `gradlew.bat`, `local.properties`, and the `gradle/` wrapper directory) have been moved into `android-app/`.
        *   The `settings.gradle.kts` within `android-app/` now correctly references its `app` module.
    *   **Root Gradle Configuration:**
        *   A new `settings.gradle.kts` has been created at the project root.
        *   This root `settings.gradle.kts` now uses `includeBuild("android-app")` to incorporate the Android project as a build in a composite build setup.
        *   `rootProject.name` in the root `settings.gradle.kts` has been set to `ChatAppProjectRoot`.
    
    **Backend Implementation (Rust with Actix Web):**
    
    *   **New `backend/` Module:**
        *   A new `backend/` directory now houses the Rust-based API server.
    *   **Actix Web Setup:**
        *   The backend is built using the `actix-web` framework.
        *   Core dependencies added to `backend/Cargo.toml`: `actix-web`, `serde` (with `derive`), `serde_json`, `dotenvy`, `log`, and `env_logger`.
        *   A basic Actix server is configured in `backend/src/main.rs`.
        *   Implemented a health check endpoint: `GET /api/health` which returns `{"status": "ok"}`.
    *   **Modular Design:**
        *   The backend source code is structured into modules for better organization:
            *   `config.rs`: Handles loading environment variables (e.g., `PORT` from a `.env` file) using `dotenvy`.
            *   `handlers.rs`: Contains request handler logic (initially, the `health_check` handler).
            *   `routes.rs`: Defines and configures API routes.
    *   **Configuration & Logging:**
        *   Logging is initialized using `env_logger`.
        *   A `backend/.env.template` file has been added (or instructions provided for its manual creation) with an example `PORT=8080`.
    
    **Build & Documentation Updates:**
    
    *   **`.gitignore` Updated:**
        *   The root `.gitignore` file has been updated to include rules for the Rust backend's build artifacts (`/backend/target/`).
        *   Paths for Android-specific ignores have been adjusted to reflect the new `android-app/` directory structure (e.g., `/android-app/build/`, `/android-app/.gradle/`).
        *   Rules for ignoring `.env` files (while allowing `.env.template` or `.env.example`) have been added.
    *   **Root `README.md` Overhaul:**
        *   The main `README.md` at the project root has been completely rewritten.
        *   It now explains the new monorepo structure, detailing the `android-app/` and `backend/` directories.
        *   Provides clear build and run instructions for both the Android frontend and the Rust backend.
        *   Project references updated to use "chatapp_project" or generic terms, with "GhostTalk" noted as the current internal name for the Android application module specifically.
    
    These changes establish a scalable foundation for developing both the frontend and backend components of the application within a unified project structure.

commit 6c3bd6917f33a610e33eeb8a53e0f7bfb0ac9470
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Sun May 25 09:22:19 2025 +0530

    feat(auth): Simplify email signup flow and consolidate account creation
    
    Refactors the email-based authentication flow to be more streamlined:
    
    - Removes `EmailEntryScreen.kt` and `PasswordEntryScreen.kt`.
    - Modifies `CreateAccountDetailsScreen.kt` to now handle email and password input directly, in addition to other user details (username, name, DOB). This screen is now the single point for creating an account via email.
    - Updates `AuthOptionsViewModel.kt` to navigate directly to `CreateAccountDetailsScreen` when "Continue with Email" is chosen for signup, passing the "signup" `flowType`.
    - Adjusts `EmailAuthViewModel.kt`:
        - Removes handling for separate email and password entry steps.
        - `SavedStateHandle` is now used to retrieve only the `flowType` for `CreateAccountDetailsScreen`.
        - Email validation is now part of the `onCreateAccountDetailsContinue()` logic.
    - Updates `MainActivity.kt` navigation graph:
        - Removes routes for the deleted `EmailEntryScreen` and `PasswordEntryScreen`.
        - Updates the route for `CreateAccountDetailsScreen` to only accept `flowType` as an argument.
    - Removes unused import statements in `MainActivity.kt` that referenced the deleted screen files.
    - Sets a static title "Create Account" in the `TopAppBar` of `CreateAccountDetailsScreen.kt` as this screen is now solely dedicated to the signup process.
    
    Known Issue:
    - The `flowType` in `EmailAuthViewModel` (and consequently the UI state) sometimes defaults to "login" even when "signup" is expected. Logging has been added to `EmailAuthViewModel`'s `init` block to investigate the `flowType` being passed via `SavedStateHandle`. This needs further investigation to ensure the correct flow is always triggered.

commit b57b2d57c1df29f907481e4448e826542f6c230c
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Sat May 24 20:09:15 2025 +0530

    feat: Task: Implemented full Email Signup and Login flow using Jetpack Compose, ViewModel, and Navigation architecture.
    Follow existing modular structure. Maintain UI/UX consistency using AppTextField, AppButton, Dimens.kt, and reusable components.

commit bb6f9fccb51de3377e55165334f65767b89bd384
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Sat May 24 11:22:36 2025 +0530

    fix(bug): fixed some issues with the UI.

commit 336694414a41ef77568fa0d9ec6bbf0085fcf4de
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Sat May 24 09:17:59 2025 +0530

    feat: Implement Authentication Options Screen and Navigation
    
    This commit introduces Phase 2 of the ChatApp development, focusing on user
    authentication pathways, navigation between screens, and essential project
    best practices.
    
    The primary addition is the "Authentication Options Screen," which is presented
    to the user after they choose to "Sign Up" or "Log In" from the initial
    Welcome Screen. This new screen provides multiple methods for authentication,
    with "Continue with Email" being the first functional (placeholder) path.
    
    Key Features & Changes:
    
    *   **Authentication Options Screen (`AuthOptionsScreen.kt`):**
        *   A new screen displaying buttons for "Continue with Email," "Continue
            with Phone," "Continue with Gmail," and "Continue with Apple."
        *   "Continue with Email" navigates to a placeholder email authentication screen.
        *   Other authentication methods currently display a Snackbar indicating
            they are TODO items.
        *   Includes KDoc documentation and a Jetpack Compose `@Preview`.
    
    *   **Navigation Implementation (Jetpack Compose Navigation):**
        *   Added `androidx.navigation:navigation-compose` dependency.
        *   `MainActivity.kt` now hosts a `NavHost` within a `Scaffold` (to support Snackbars).
        *   Defined navigation routes: "welcome," "auth_options," and "email_auth."
        *   `WelcomeScreen.kt` buttons now navigate to the "auth_options" route.
    
    *   **Animated Screen Transitions:**
        *   The platform name ("GhostTalk") and slogan ("Private. Secure. Modern.")
            on the `WelcomeScreen` now animate out (slide up/fade) when navigating
            to the `AuthOptionsScreen` and animate back in when returning.
        *   This is achieved using `AnimatedVisibility` in `MainActivity.kt`'s
            `ChatAppNavigation` composable, which conditionally displays these elements
            based on the current navigation route.
        *   `WelcomeScreen.kt` was modified to conditionally render its own title/slogan
            to support this shared animation scheme.
    
    *   **Project Hygiene & Best Practices:**
        *   **`.gitignore`:** Added a comprehensive `.gitignore` file tailored for
            Android, Kotlin, Gradle, and common IDE/system files.
        *   **`README.md`:** Created a detailed `README.md` including:
            *   Project name ("ChatApp by aarchangel") and overview.
            *   Technologies used.
            *   Build instructions.
            *   Explanation of the project structure.
            *   A section for the future roadmap.
        *   **`LICENSE`:** Added an MIT License file.
        *   **`CODE_OF_CONDUCT.md`:** Included the Contributor Covenant Code of Conduct.
        *   **`CONTRIBUTING.md`:** Provided basic guidelines for contributing to the project.
        *   **KDoc Documentation:** Added extensive KDoc comments to all new and
            significantly modified Composables, functions, classes (including
            `MainActivity.kt`, `WelcomeScreen.kt`, `AuthOptionsScreen.kt`, `AppConfig.kt`,
            and all files in `ui/theme/`).
        *   **Previews:** Ensured `@Preview` annotations are present for all major screens
            (`WelcomeScreen`, `AuthOptionsScreen`, `MainActivity`'s navigation).
    
    *   **Code & Conventions:**
        *   Adhered to universal Kotlin coding conventions.
        *   Maintained separation of concerns where appropriate for current complexity.
        *   Ensured file naming and directory structure align with specified conventions
            (e.g., `ui/screens/`).
    
    This phase significantly enhances the application's groundwork by establishing
    navigation, adding a key step in the user authentication flow, and incorporating
    crucial documentation and best-practice files for future development and
    collaboration.

commit dc4404dbbc364c73f0569964779cf01e9976f776
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Fri May 23 21:56:39 2025 +0530

    feat: Implement initial project structure and Welcome Screen for ChatApp
    
    This commit establishes the foundational structure for the ChatApp Android application,
    built with Jetpack Compose. It introduces the primary welcome screen, core
    configuration, and necessary theming elements, alongside a comprehensive project
    setup.
    
    The project, internally identified as "ChatApp by aarchangel," currently
    presents itself to the user as "GhostTalk" (this display name is configurable
    and can be updated in the future without altering the core codebase identifiers).
    
    Key features and changes in this commit:
    
    *   **Project Initialization:**
        *   Configured root and app-level `build.gradle.kts` files with necessary
            Android, Kotlin, and Jetpack Compose dependencies.
        *   Set up `settings.gradle.kts` and `gradle.properties` for project
            configuration, including AndroidX and Jetifier support.
        *   Generated and configured the Gradle wrapper (`gradlew`).
        *   Created a basic `AndroidManifest.xml` with necessary permissions,
            application definition, and MainActivity declaration.
        *   Added essential Android resources: `strings.xml` for app name,
            `themes.xml` for the base XML theme, placeholder icons, and
            data/backup extraction rules.
    
    *   **Core Configuration (`AppConfig.kt`):**
        *   Created `AppConfig.kt` under `com.aarchangel.chatapp.config` to hold
            centralized application constants.
        *   Defined `PLATFORM_NAME` (currently "GhostTalk"), chat type names,
            default theme, and guest mode status.
        *   Includes an author comment "ChatApp by aarchangel".
    
    *   **Welcome Screen (`WelcomeScreen.kt`):**
        *   Developed `WelcomeScreen.kt` using Jetpack Compose under
            `com.aarchangel.chatapp.ui.screens`.
        *   Features a full-screen dark background.
        *   Displays a centered large title (from `AppConfig.PLATFORM_NAME`),
            a subtitle ("Private. Secure. Modern."), and two prominent buttons:
            "Sign Up" and "Log In".
        *   Styled with Material 3 components, rounded corners, elevation,
            and proper spacing.
        *   Includes TODOs for navigation in button `onClick` handlers.
        *   Accompanied by a `@Preview` function for UI development.
    
    *   **Theming (`com.aarchangel.chatapp.ui.theme`):**
        *   Created `ChatAppTheme.kt` (renamed from GhostTalkTheme) providing a
            Jetpack Compose theme, supporting dynamic colors on Android 12+.
        *   Defined `Color.kt` with default color palettes (Purple80, Pink40, etc.).
        *   Established `Type.kt` with basic typography settings.
        *   All theme files include the "ChatApp by aarchangel" author comment.
    
    *   **Main Activity (`MainActivity.kt`):**
        *   Created `MainActivity.kt` under `com.aarchangel.chatapp` as the
            entry point of the application.
        *   Sets its content to the `WelcomeScreen` composable, wrapped in
            `ChatAppTheme`.
        *   Includes an author comment "ChatApp by aarchangel".
    
    *   **Refactoring and Naming Convention:**
        *   The project's base package name and `applicationId`/`namespace` have been
            set to `com.aarchangel.chatapp`.
        *   Internal Kotlin class names (e.g., `ChatAppTheme`) and comments reflect
            "ChatApp by aarchangel".
        *   User-facing names (like the app name in the launcher and the title on
            the welcome screen) currently use "GhostTalk" as defined in `AppConfig.kt`
            and `strings.xml`, allowing for future changes to the display name
            without altering the underlying codebase structure.
    
    This initial setup provides a solid foundation for further development of the
    ChatApp, focusing on a clean, modern, and privacy-first approach.

commit 66f1cb3249f7652f7a8cd7b72c3cac5bae97f491
Author: Mohammad Aslam <aslammohammad336@gmail.com>
Date:   Fri May 23 20:48:36 2025 +0530

    Initial Commit.
